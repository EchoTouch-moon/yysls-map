# 燕云十六声剧情关系图谱：项目现状与发展路线

> **当前生效状态（2026-08-23）**：本文保留此前 MVP / 发布路线作为历史基线；自本区块起，产品当前执行方向由 **Wave 1.6 — Narrative Depth & Native Presentation** 接管。Wave 1.5 已完成 canonical story alignment、canonical v0.1 data population、canonical-first continuous Timeline 与 H-D1 deep-link overlay parity；真实玩家反馈确认 canonical alignment 方向正确，但整体 Product Validation 尚未通过。当前瓶颈从“故事顺序是否对齐游戏”转为“剧情研究是否足够深、明潮/暗涌是否完整、两种阅读模式是否统一、人物 recall 与玩家语言是否自然、视觉是否真正对齐燕云”。
>
> 当前执行优先级：**E0 Narrative Research Expansion + E0.5 Local Asset Feasibility（只读）**。Mac 作为 research/product hub；Windows 作为 game evidence station。先把“第一章·神仙不渡 / 又见新来燕”做成最终质量 pilot，再决定 Narrative Model v0.2 与 Reading UX v2；Canvas、开封、AI chat、账号/收藏、大规模解包继续 DEFER。
>
> - 当前方向：[`docs/WAVE_1_6_NARRATIVE_DIRECTION.md`](docs/WAVE_1_6_NARRATIVE_DIRECTION.md)
> - 双机任务分发：[`docs/execution/WAVE_1_6_DUAL_MACHINE_TASKS.md`](docs/execution/WAVE_1_6_DUAL_MACHINE_TASKS.md)
> - 执行台账：[`docs/execution/TASKS.md`](docs/execution/TASKS.md)
>
> 下方 2026-08-20 / 2026-08-21 内容用于保留演进历史；若与上述 Wave 1.6 状态冲突，以本区块与 Wave 1.6 方向文件为准。

> 评估日期：2026-08-20。本文把工程完成度、内容准备度和发布准备度分开判断；功能存在不等于产品已经可发布。

## 一、执行摘要

项目已经是一个可运行、可测试的工程 MVP，而不是概念原型。关系图、时间线、人物/势力卷宗、搜索与关系路径、投稿审核、内容管理、防剧透策略、迁移、导入审计和部署配置都已具备。时间线也已经从单纯的事件索引扩展为一条人工编排的清河主线导读，并用三张独立史料卡验证了“剧情—历史语境—来源核对”的最小闭环。当前真正的瓶颈已经从“能不能做出来”转为“内容能否让用户安全地相信并使用”。

当前阶段应定义为：**清河单章的工程 MVP 与核心信任闭环已完成，正在进入编辑审校与预发布准备期**。

近期不应继续横向增加功能。本轮完成的故事导读属于“把故事讲清楚”的核心能力，三张历史卡只是受控样板，不代表应扩成历史百科。下一优先级仍是清河内容审校、来源升级、真实玩家理解度验证、远程 CI、隔离预发布、备份恢复和上线演练。开封扩容、多故事线、完整历史双轴、账号体系、收藏、推荐、AI 自动整理都应等待首章发布闭环跑通后再评估。

## 二、当前项目画像

### 2.1 产品定位

面向《燕云十六声》玩家的非官方剧情理解工具。核心差异不是“资料数量最多”，而是：

- 以人物关系和事件因果帮助玩家理解复杂叙事；
- 按玩家已经完成的剧情节点限制响应，降低剧透风险；
- 区分明确事实、可信整理和待考解读；
- 保留来源与人工审核边界，不建设原文、对白或拆包数据库。

### 2.2 已有能力

- Web：Next.js、TypeScript、React Flow，包含关系图、双模式剧情时间线、人物/势力页、搜索、最短关系路径、投稿和管理台。
- API：FastAPI、SQLAlchemy、PostgreSQL，包含统一响应、内容状态、防剧透过滤、故事导读、历史背景关联、限流、管理员会话与 CSRF 防护。
- 内容工程：结构化 JSON 合约、故事路线与历史资料的独立实体、迁移、非破坏性导入、校验、dry-run、显式替换确认、事务锁和导入审计。
- 质量保障：Ruff、mypy、ESLint、TypeScript、pytest、Vitest、Playwright、CI 和部署手册。

### 2.3 当前数据

清河 v4 包含 1 个章节、10 个势力、39 个角色、27 个事件、29 条关系和 21 个作品来源定义，另有 1 条 10 节点的主线导读、3 张历史背景卡、3 条剧情—历史关联和 6 条独立历史参考。作品来源中的 21 条仍全部为 `community_analysis`；新增史料只能支撑对应历史卡，不能反向替代游戏剧情的官方资料或任务定位。

### 2.4 准备度判断

| 维度 | 当前判断 | 主要证据 | 下一道门槛 |
| --- | --- | --- | --- |
| 工程 MVP | 已完成 | 全量静态检查、真库测试、迁移往返、E2E 和生产构建通过 | 保持主分支绿色并把同等门禁接入远程 CI |
| 产品核心 | 已完成当前闭环 | 图谱、时间线、检索、防剧透、进度语义和公开依据可用 | 用真实玩家验证可理解性与剧透安全 |
| 内容发布 | 未完成 | 清河覆盖已有规模，但来源单一且高剧透记录待审 | 编辑签字、来源分级与发布清单 |
| 发布运维 | 未完成 | 有 CI/部署配置和 runbook | 真实远程 CI、staging、备份恢复与回滚演练 |
| 增长扩张 | 不应开始 | 尚无公开版使用数据 | 首章稳定发布并获得真实反馈 |

## 三、核心用户闭环

最短价值路径应当是：

1. 用户选择自己**已经完整通关**的剧情节点。
2. 从一个人物、事件或搜索词进入。
3. 沿可见关系、完整事件或人工导读理解目标、阻力、转折与因果。
4. 看清该结论属于作品事实、历史事实、可信对照还是编辑推测。
5. 打开对应来源，核对依据后继续探索。

审计时第 2—4 步已经可用，第 1 步和第 5 步存在缺口：公开选择器展示了尚无对应内容的“开封篇”，且“当前进度”实际等同于全部高剧透内容；数据层保存了来源，但主要公开页面没有展示来源。本轮已将公开节点收敛为三个真实可区分状态，并在关系、人物和时间线补齐防剧透来源摘要，因此这条最短闭环现已在工程层面打通。

## 四、问题与风险

### P0：发布前必须解决

1. **进度语义存在误导（本轮已完成）。** 原 5 个选项只有 3 个不同数据结果；现已收紧选项，并明确章节节点代表已经完整通关。
2. **证据链在公开端中断（本轮已完成）。** 可信度数字无法替代来源；关系、人物和时间线现已展示与当前可见实体绑定的防剧透来源摘要。
3. **内容尚未完成编辑签字。** 剧透等级 2/3、可信度低于 0.9、隐藏关系、身份解释和争议性措辞必须逐条人工判断。此项不能由代码自动代替。
4. **来源类型单一。** 社区文章适合作为线索，不应独自承担全部高风险结论；应补充任务定位、官方材料或明确的玩家亲历记录。

### P1：上线前必须解决

1. 仓库没有配置远程与上游，真实 CI 状态不可验证。
2. 尚无 staging 发布、备份恢复实测和生产式 smoke test 证据。
3. 默认本地验证会跳过两项 PostgreSQL 测试，应在 CI 和发布流程中强制运行数据库测试。
4. 当前只有清河一章，不能用“功能支持未来章节”替代真实内容覆盖。

### P2：可在首发后改进

- 图谱在移动端降级为引导文案；是否需要关系列表式替代，应由移动端使用数据决定。
- 缺少面向内容编辑的来源覆盖、待审记录和高风险内容质量看板。
- 搜索、来源打开和探索完成情况没有匿名产品指标，无法判断用户真正卡在哪里。

## 五、功能分层

### 5.1 核心功能

- 安全、明确的已通关进度选择。
- 防剧透的图谱、时间线、人物详情、搜索和关系路径。
- 事实/整理/解读分层与用户可见的来源证据。
- 一条经过人工编排、全链路防剧透的清河主线导读。
- 内容投稿、人工审核、结构化发布和可回滚导入。
- 清河发布候选的编辑审校、staging 和恢复演练。

### 5.2 增强功能

- 第二个真实章节出现后，由已发布章节动态生成进度选项。
- 势力/事件完整卷宗、来源类型筛选和内容质量看板。
- 在当前三张样板通过读者验证后，再逐步补充少量高价值历史背景卡。
- 基于真实移动端需求的紧凑关系列表。
- 空搜索提示、相关人物入口和更清晰的探索续接。

### 5.3 未来方向

- 清河发布流程稳定后再扩展开封及后续章节。
- 多故事线、跨章节路线和“剧情轴 / 历史轴”双轴对照；只有单线导读验证有效后才设计。
- 历史背景与地图联动；先验证读者确实需要空间理解，再承担地名考据与年代边界成本。
- 经用户需求验证后再考虑书签、阅读状态、个人笔记和跨设备同步。
- 建立最小化、隐私友好的产品指标，观察进度选择、搜索成功、来源打开和投稿转化。

### 5.4 灵感仓库

- AI 自动发布、个性化推荐、社交信息流、实时多人编辑和复杂账号成长体系。
- AI 自动考据、自动把剧情绑定为历史原型或无需人工审核的背景卡发布。
- 这些方向会显著增加版权、审核、隐私和信任成本，当前没有证据表明它们比内容质量与发布稳定性更重要，因此不进入近期实现。

## 六、分阶段路线图

### 阶段 A：核心信任闭环（已完成）

- 公开端只保留真实支持的进度节点，并明确“章节已通关”。
- 在关系、人物和时间线展示 spoiler-safe 来源摘要。
- 保持已有图谱与安全导入工作绿色，完成真实数据库与页面走查。

退出条件已满足：三个主要阅读面都能从结论到达来源；公开选项不再映射到重复或误导的数据范围；全量验证、真实数据库测试、迁移往返与隔离 E2E 均通过。

### 阶段 B：清河编辑发布候选

- 审阅全部剧透等级 2/3、低于 0.9 可信度、隐藏关系、身份解释和争议性措辞。
- 为高风险结论补任务定位、官方材料或明确玩家亲历；无法确认的记录降级、改写或暂不发布。
- 用真实玩家验证 10 节导读能否回答“主角在追什么、什么改变了他、历史背景为何重要”，并逐字审阅导读桥接与下一问。
- 保持历史卡与作品事件分离；新增历史断言必须有独立来源、事实标签和边界说明。
- 建立逐条签字清单，冻结一个可复现的数据集版本和文件哈希。

退出条件：每条公开内容有明确审阅决定；高风险结论不只依赖单一社区转述；清河数据集可重复导入且结果一致。

### 阶段 C：预发布与运维闭环

- 配置 Git 远程并让真实 CI 强制执行迁移、数据库测试、全量验证和 E2E。
- 部署隔离的 API、数据库和 Web 预览环境。
- 完成迁移、导入、备份、恢复、回滚和生产式只读 smoke test。

退出条件：同一修订可从空库自动生成发布候选；备份能恢复；回滚步骤经过演练；没有默认演示数据进入真实环境。

### 阶段 D：小范围公开测试

- 邀请少量真实玩家完成“设进度—找人物—理解关系—查来源”任务。
- 记录剧透事故、空搜索、误解、来源失效和投稿处理时长。
- 只修复真实阻塞，不根据零散想法横向扩功能。

退出条件：核心任务可稳定完成；没有已知 P0 剧透泄漏；内容修订和投稿审核有可持续节奏。

### 阶段 E：章节扩张

- 只有阶段 B—D 的流程可重复后，才整理开封数据。
- 此时再把公开进度选择改为由已发布章节驱动，并评估跨章节关系演化、差异视图和更细粒度里程碑。

退出条件：新增章节不降低来源覆盖、审核标准、防剧透测试或发布可回滚性。

## 七、已完成的改进

### 7.1 核心信任闭环

1. 将公开进度选择收敛为当前真实支持的状态，并用“已完整通关”语义替代模糊的章节名。
2. 将已有来源关联以折叠、低干扰的方式展示在关系详情、人物卷宗和时间线事件中；外部链接只允许 HTTP(S)。
3. 按审计结果升级存在已知漏洞的生产与开发依赖，并在项目声明的 Node 24 和 Python 3.13 环境重新验证。

### 7.2 故事导读与历史背景垂直切片

1. 在同一 `/timeline` 页面提供默认“跟着故事读”和“完整事件”两种模式，不另造重复导航入口。
2. 人工选择 10 个清河关键节点，逐节提供叙事角色、发生了什么、为什么重要、前后承接和继续追问，并复用人物、关系和作品证据。
3. 为“作品中的中渡桥之战”增加三张独立历史卡，显式展示年代、事实类型、关联性质、边界说明、编辑说明和可定位的历史来源。
4. 把故事路线、历史背景、剧情—历史关联和历史参考建成独立模型，并让 arc、beat、event、relationship、history link、history context 全部经过进度与剧透过滤。
5. 将既有“945 年、20 万守桥、事先勾结并蓄意害死”的混合断言纠正为可核验的 946 年史事、兵力记载分歧和动机证据边界。

本轮仍不实现多故事线、完整历史双轴、地图联动、开封内容、账号、收藏、分析埋点、移动图谱或 AI 自动整理。

验证实绩：41 个 Web 测试、36 个 PostgreSQL API 测试、2 个桌面/移动 E2E、生产构建和 Alembic 降级—升级往返全部通过；npm 与 Python 依赖审计无已知漏洞。

## 八、指标与质量门槛

### 内容与信任

- 已发布关系、人物和事件的来源可达率：100%。
- 高风险结论的编辑签字率：100%。
- 只含单一社区转述的高风险结论：发布前降至 0，或明确标为待考并不公开。
- 失效来源链接：发布检查中为 0。
- 已发布历史卡的独立历史来源覆盖率：100%；无来源历史卡在导入阶段直接拒绝。

### 防剧透

- 每个公开进度节点都有独立的 API 矩阵测试和真实数据断言。
- 降低进度时，旧的高进度内容立即从界面移除。
- 导读节点、关系、历史卡和历史来源与对应事件一起裁剪，不留下标题或说明残片。
- 公开选择器不存在无内容、重复含义或默认放宽的进度项。

### 工程与发布

- Lint、类型检查、单测、生产构建全部通过。
- PostgreSQL 生命周期测试、迁移 upgrade/downgrade/upgrade 和隔离 E2E 在 CI 中强制通过。
- npm 全量审计与 Python 依赖审计无已知漏洞。
- 发布前完成可验证的备份恢复和回滚演练。

### 产品验证

- 测试用户能在不求助的情况下设置正确进度并找到指定人物。
- 用户能解释关系的资料性质并打开至少一个依据。
- 用户读完清河导读后，能复述主角目标、至少两个关键转折，并分清中渡桥卡中的史实与作品虚构。
- 重点跟踪空搜索率、来源打开率、核心任务完成率和剧透事故数；在有真实基线前不设虚构增长目标。

## 九、产品方向转向与 Wave 计划（2026-08-21 决议）

### 9.1 主命题调整

产品核心从「防剧透的剧情关系图谱」调整为「帮助玩家真正理解故事的剧情知识图谱」：

> **知道更多 ≠ 被剧透。**

防剧透降级为一种阅读能力（保护层），不再作为系统组织原则。第一优先用户是
已通关玩家（把故事讲透）；首次游玩玩家获得背景但不接触重大揭示。
本节与第五、六节冲突时，以本节为准。

### 9.2 Wave 1 — IMPLEMENTED / VALIDATION_PENDING

状态：实现完成并通过代码级复核（含 H1–H4 hardening），等待真实用户验证；
验证未完成前不标记 CLOSED。

范围：派生分层 + 人物剧情足迹 + Story Guide 中枢化 + 人物级 Reveal +
Story-first 首页 + 历史背景次级入口。

工程边界：`No persistent-model change / No dataset mutation / No schema
migration`；允许 API projection、derived aggregation、contract additive change。

验收门（Acceptance Gates）：

- **G1** Story-first 首页；Progress 不占 Hero 核心位置
- **G2** 剧情足迹由现有 StoryArc/Event 数据派生，不改冻结数据集
- **G3** 人物页按「初识 → 剧情足迹 → 联系 → 完整解析 → 背景」组织
- **G4** 默认可见范围不扩大；完整解析仅由人物级显式 Reveal 触发。
  语义边界（准确表述）：Reveal 是 **UI 呈现层门控 + 人物级 unrestricted 阅读上下文**，
  不是字段级数据边界——服务器载荷中 `interpretation` 在人物整体可见时即已返回，
  保证的是「用户界面不在显式操作前呈现完整解析」。字段级
  （intro/analysis/revelation）权限模型属 Wave 2
- **G5** Beat → 人物 → 关系图/历史 → 返回故事，深链连续
- **G6** `qinghe-chapter-1-v5` 及其 SHA、编辑签字不因 UI/projection 改动而变化

Hardening（复核后已补）：

- **H1** `/history/{slug}` related beats 补齐 Link→Beat→Event→Arc 四层可见性闭包，
  并附回归 fixture（安全卡 × 未来事件链：START 下卡可见而 `related=[]`）
- **H2** `story_path` 补 StoryArc 父级 publish/visibility 闭包，附回归 fixture
  （DRAFT 卷不泄漏已发布子节点；转 PUBLISHED 后足迹出现）

语义约定：自动聚合的人物事件序列一律称「剧情足迹」；
人工撰写的人物叙事（Wave 2）才称「人物线/人物故事」。

### 9.3 Wave 2 候选能力（BACKLOG / VALIDATION_REQUIRED）

进入条件 = Wave 1 上线后的真实使用证据，当前一律不实施：

| 候选 | 说明 | 进入条件 |
| --- | --- | --- |
| 伏笔与回看 | beat 级 foreshadows 字段（需 schema 1.2 + 数据集重冻结） | 导读完成率与回访数据支持 |
| 问题驱动探索 | 策展问题（如「江晏为什么失踪」）自动组装答案页 | 搜索/空搜索数据显示问题式需求 |
| 游玩前导读 | 叙事准备内容，复用历史卡 + 势力索引 | 新玩家留存验证 |
| 人工人物线 | 10–15 个核心角色的专属叙事 | 剧情足迹使用率证明入口价值 |
| 字段级 Reveal | intro/analysis/revelation 分权 | 人物级 Reveal 点击率达标 |

明确不做（本轮）：schema 1.2、foreshadows、人工人物线、问题引擎、
新 `/read` 路由、字段级 Reveal、更细 Progress 状态。
